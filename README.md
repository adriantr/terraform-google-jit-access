# terraform-google-jit-access
Terraform module for [JIT access implementation](https://github.com/GoogleCloudPlatform/jit-access).

It currently supports one project, but will be expanded to support folder.
